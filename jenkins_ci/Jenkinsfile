pipeline { 
    agent {
        label 'docker'
    } 
    stages {
        stage('Structure of directories'){
            steps {
                sh 'tree \$PWD'
            }
        }
        stage('Molecule test'){
            steps {
                dir('playbook/roles/elasticsearch-role') {
                    sh 'molecule test'
                }                
            }                        
        }                
    }
}